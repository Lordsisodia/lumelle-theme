{% comment %} Lumelle PDP Gallery React Mount Point {% endcomment %}

<div id="react-pdp-gallery" class="lumelle-pdp-gallery-section" data-handle="{{ product.handle }}" data-product="{{ product | json | escape }}"></div>

{% schema %}
{
  "name": "PDP Gallery",
  "settings": [],
  "presets": [
    {
      "name": "Lumelle PDP Gallery (React)"
    }
  ]
}
{% endschema %}
